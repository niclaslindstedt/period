---
type: Changed
title: The installed app fills the phone screen
---

The app bar's redundant "Period" title is gone — its rule now sits directly under the status bar — and the bottom navigation clears the home indicator by a hair instead of a full inset, so an installed iOS app paints edge to edge with no dead band at the bottom.
