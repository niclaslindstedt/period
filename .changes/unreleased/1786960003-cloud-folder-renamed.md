---
type: Changed
title: The cloud folder is now nird-cycle
---

The synced document is filed under `nird-cycle` rather than `Cycle` in Dropbox and Google Drive, so an account that synced before the rename looks empty until the old file is moved across — nothing is deleted, and moving it back in merges rather than replaces. [Learn more](feature:cloud-sync)
