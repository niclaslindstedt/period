---
type: Fixed
title: The update prompt no longer covers the tabs
---

The "a new version is ready" card now sits just above the bottom navigation instead of on top of it, so the four tabs stay reachable while an update waits.
